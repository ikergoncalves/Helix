# Public base URL of the deployed site.
# Consumed by `metadataBase` and the Open Graph tags in src/app/layout.tsx.
# Optional locally — it falls back to http://localhost:3000 when unset.
# In production set this to the real deployment URL (e.g. your Vercel domain).
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
